---
- name: Install ZooKeeper's archive
  get_url:
    url: "{{ archives.zookeeper[zookeeper_version].url }}"
    dest: /tmp/zookeeper.tar.gz

- name: Unarchive a ZooKeeper
  unarchive:
    src: /tmp/zookeeper.tar.gz
    dest: /tmp
    remote_src: yes
